{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for XDebug",
            "type": "php",
            "request": "launch",
            "port": 9012,
            "pathMappings": {
              "${userHome}/Playground/php_test/example-app": "${workspaceFolder}",
            }
        }
    ]
}